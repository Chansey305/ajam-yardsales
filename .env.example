# VAPID keys — generate with: npm run generate-vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# Shared secret for /admin notify/test and /api/calendar-hook
ADMIN_SECRET=change-me-to-a-long-random-string

PORT=3000
DATABASE_PATH=./data/subscriptions.db
